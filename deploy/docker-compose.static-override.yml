services:
  caddy:
    volumes:
      - ./deploy/Caddyfile.100waytoai.docker:/etc/caddy/Caddyfile:ro
      - ./baizhi-static:/srv/baizhi-students:ro
      - caddy_data:/data
      - caddy_config:/config
